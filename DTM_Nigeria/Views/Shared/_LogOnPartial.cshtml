@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log Off", "LogOff", "Account") ] [ @Html.ActionLink("Chg Passwd", "ChangePassword", "Account") ]</text>
    
    if(User.IsInRole("Admin")){
    <text> [ @Html.ActionLink("New User", "Register", "Account") ] </text>
    }
}
else
{
    @:[ @Html.ActionLink("Log On", "LogOn", "Account") ] [ @Html.ActionLink("Register", "Register", "Account") ]
}
